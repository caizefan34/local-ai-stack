# Knowledge Base Sync Configuration
# Copy to config.env and adjust for your environment

# WSL paths (Linux paths inside WSL)
KB_HOME=~/knowledge-base

# Windows paths (from WSL's /mnt/ mount)
KB_WINDOWS_SOURCE=/mnt/d/knowledge
KB_WINDOWS_REF_SOURCE=/mnt/d/references

# FastGPT settings
FASTGPT_URL=http://localhost:3000
FASTGPT_DATASET_NAME=knowledge_base

# Optional AST-aware indexing for source repositories. The generated JSON keeps
# functions/classes, docstrings, imports, call names, and dependency edges.
CODE_INDEX_ENABLED=false
LOCAL_AI_STACK_ROOT=/path/to/local-ai-stack
# CODE_INDEX_ROOT=/path/to/source-repository
